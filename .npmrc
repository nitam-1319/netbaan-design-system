# Route the @netbaan-project scope to GitHub Packages (private) for publish/install.
# The token is supplied via the NODE_AUTH_TOKEN env var — no secret is committed.
@netbaan-project:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
