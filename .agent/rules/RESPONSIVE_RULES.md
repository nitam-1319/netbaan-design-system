# RESPONSIVE_RULES.md — Responsive behavior

Every component works across Mobile, Tablet, Desktop, and Large Desktop.

Verify: layout behavior at each breakpoint, touch-target size (min 44×44px on touch),
overflow handling, text wrapping, density, and interaction usability.

Breakpoints come from tokens (`../rules/TOKEN_RULES.md`), not literal media-query widths.
